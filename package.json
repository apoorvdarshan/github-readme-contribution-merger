{
  "name": "github-readme-contribution-merger",
  "version": "1.0.0",
  "description": "Merge GitHub contribution graphs for multiple users into a single SVG heatmap",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  },
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260710.1",
    "@types/node": "^26.1.1",
    "typescript": "^5.3.0",
    "wrangler": "^4.110.0"
  }
}
