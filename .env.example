# GitHub personal access token (required for GraphQL API)
# Create one at: https://github.com/settings/tokens
# No special scopes needed — public read access is sufficient
GITHUB_TOKEN=ghp_your_token_here
